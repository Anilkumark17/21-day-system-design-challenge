{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "cron:digest": "node -e \"require('dotenv').config(); require('./src/cron/dailyDigest.job').runDailyDigest()\"",
    "cron:cleanup": "node -e \"require('dotenv').config(); require('./src/cron/cleanupMessages.job').runMessageCleanup()\"",
    "cron:stats": "node -e \"require('dotenv').config(); require('./src/cron/stats.job').runStatsLog()\""
  },
  "dependencies": {
    "@neondatabase/serverless": "^1.1.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.45.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "node-cron": "^4.6.0",
    "nodemon": "^3.1.14",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.10"
  }
}
